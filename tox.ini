[tox]
isolated_build = True
skip-missing-interpreters = True

[testenv]
deps =
  coverage
  pytest
  pytest-cov
  pre-commit
  -rrequirements.txt
commands =
  pre-commit run --all-files --show-diff-on-failure
  coverage erase
  pytest -v --cov=magicformulabr --cov-report=term-missing --cov-report=xml
  coverage html
